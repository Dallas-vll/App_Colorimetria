// JavaScript Example: Reading Entities
// Filterable fields: photo_url, skin_undertone, skin_depth, skin_intensity, season, temperature_score, depth_score, intensity_score, contrast_score, questionnaire_data, primary_colors, neutral_colors, accent_colors
async function fetchUserAnalysisEntities() {
    const response = await fetch(`https://app.base44.com/api/apps/68e6ee736618adaaff50b002/entities/UserAnalysis`, {
        headers: {
            'api_key': 'e0779cdc25ff4f46ab24ba8e5be858c0', // or use await User.me() to get the API key
            'Content-Type': 'application/json'
        }
    });
    const data = await response.json();
    console.log(data);
}

// JavaScript Example: Updating an Entity
// Filterable fields: photo_url, skin_undertone, skin_depth, skin_intensity, season, temperature_score, depth_score, intensity_score, contrast_score, questionnaire_data, primary_colors, neutral_colors, accent_colors
async function updateUserAnalysisEntity(entityId, updateData) {
    const response = await fetch(`https://app.base44.com/api/apps/68e6ee736618adaaff50b002/entities/UserAnalysis/${entityId}`, {
        method: 'PUT',
        headers: {
            'api_key': 'e0779cdc25ff4f46ab24ba8e5be858c0', // or use await User.me() to get the API key
            'Content-Type': 'application/json'
        },
        body: JSON.stringify(updateData)
    });
    const data = await response.json();
    console.log(data);
}